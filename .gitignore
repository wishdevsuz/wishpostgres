node_modules
dist
dist-ssr
dist-release
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
src-tauri/target
src-tauri/gen

# package-lock.json is deliberately tracked: `npm ci` needs it, and a release
# should install the exact dependency tree that was tested.
